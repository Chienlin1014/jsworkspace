// 7. 請定義函式power，說明如下
// 功能: 計算次方值
// 參數
// a: 底數
// b: 次方數
// 回傳: 次方值

/**
 *
 * @param {number} a 底數
 * @param {number} b 次方數
 * @returns {number}
 */
function power(a, b) {
    return a**b;
}

console.log(power(2, 2));